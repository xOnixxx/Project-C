using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyDestroy : ParticleDestroy
{
    
    // Start is called before the first frame update
    void Start()
    {
        isFragile = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
